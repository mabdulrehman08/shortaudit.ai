# API contracts

The app uses Next.js App Router API routes under `app/api/*` for serverless deployment on Vercel. This folder stores shared TypeScript contracts for clients, server actions, and future SDK generation without introducing a separate backend service.
